# Core

This directory owns shared browser-side foundations once the public site grows
beyond standalone pages: configuration, status modelling, logging interfaces,
storage adapters and reusable accessibility/performance utilities.

It must not contain provider secrets, private OAuth token handling or
feature-specific presentation logic.
