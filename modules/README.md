# Modules

This directory owns independently understandable user-facing capabilities such
as a landing-page feature, connection-status presentation or content discovery
view.

Each introduced module must state its user outcome, dependencies, public route
impact, loading/error states and validation approach before it is merged.
