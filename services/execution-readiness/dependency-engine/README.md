# Dependency Engine

Models dependency checks required before a future execution plan can be considered ready.

Dependencies are read-only references to contracts, documents, configuration, runtime models, and governance records. A satisfied dependency does not grant execution permission.
