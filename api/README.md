# API Boundary

This public repository may contain browser-safe interface definitions and
connection-status presentation contracts only.

OAuth code exchange, refresh tokens, API secrets, privileged analytics and
authenticated posting must execute in a protected service with encrypted secret
storage, expiry/reconnect handling, rate-limit backoff and audit logging.
